name: "E2ETests"

on: [push, pull_request]

jobs:
  analyze:
    runs-on: ubuntu-22.04 # needed for a newer docker-compose version

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    - name: Run tests
      run: |
        ./util/test.py
